---
title: Production Compilers
description: torch.compile, JAX/Pallas, IREE/ExecuTorch, operator fusion — the compilers people actually run in production.
---

# Production Compilers

The compilers your workloads actually flow through. `torch.compile` is the default in PyTorch 2.x; JAX/XLA powers Google scale; IREE/ExecuTorch ships to mobile. This module is how each one composes the [foundation passes](../foundation) into a deployable artifact.

<ModuleProgress
  lessons={[
    { slug: '/compilers/production/torch-compile', title: 'torch.compile', estMin: 16 },
  ]}
/>

> Lessons coming: JAX & Pallas · IREE & ExecuTorch · Operator Fusion
