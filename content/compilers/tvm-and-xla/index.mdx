---
title: TVM, XLA & ExecuTorch
description: The ML compiler frontends that consume PyTorch / JAX / TF and emit optimized binaries.
---

# TVM, XLA & ExecuTorch

The user-facing end of the ML compiler stack. These are the tools you'll actually run when you want to take a model and deploy it: `torch.compile` (Inductor), JAX's XLA, Apache TVM, ExecuTorch for edge.

<ModuleProgress
  lessons={[
    { slug: '/compilers/tvm-and-xla/torch-compile', title: 'torch.compile (Inductor)', estMin: 14 },
  ]}
/>

> More lessons coming: JAX & Pallas · IREE & ExecuTorch · Operator Fusion deep dive

<Capstone
  title="Find the fusion that mattered with torch.compile"
  pitch="The same code, 2× faster — because Inductor fused two ops into one read of HBM. Find which two."
  what="Take an attention block. Run with eager PyTorch. Run with `torch.compile`. Measure step time. Then dump Inductor's generated Triton kernel (`TORCH_LOGS=output_code`) and find the fused kernel that closed the gap. Diff against the eager version. Write a 1-paragraph explanation of the win."
  sota={['PyTorch 2.5+', 'torch.compile (TorchInductor)', 'Triton (Inductor codegen target)', 'torch.profiler', 'TORCH_LOGS=output_code']}
  device="colab"
  time="One evening"
  level="intermediate"
/>
