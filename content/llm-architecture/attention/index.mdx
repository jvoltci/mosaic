---
title: Attention
description: MHA, GQA, MQA, MLA, RoPE, and FlashAttention — the attention block from a memory-and-compute lens.
last_reviewed: 2026-04-26
---
# Attention

Attention is the heart of the Transformer. It's also the most memory-hungry, latency-sensitive, and innovation-rich block — which is why most production tricks (FlashAttention, GQA, MLA, RoPE) target it specifically.

<ModuleProgress
  lessons={[
    { slug: '/llm-architecture/attention/mha', title: 'Multi-Head Attention', estMin: 14 },
    { slug: '/llm-architecture/attention/gqa-mqa-mla', title: 'GQA, MQA & MLA', estMin: 14 },
    { slug: '/llm-architecture/attention/rope-yarn', title: 'RoPE + YaRN / LongRoPE', estMin: 14 },
    { slug: '/llm-architecture/attention/flash-attn-3', title: 'FlashAttention-3', estMin: 16 },
  ]}
/>

<Capstone
  title="Implement attention from scratch — including the FlashAttention tile"
  pitch="The attention block as it actually runs in production, written, verified against Llama-3.2-1B."
  what="A single-file PyTorch implementation: multi-head, GQA, RoPE, and a tiled FlashAttention-style forward pass. Outputs match Llama-3.2-1B's attention layer to numerical tolerance. Benchmark plot at seq_len 4096 showing tiled-vs-naive HBM traffic."
  sota={['PyTorch 2.x', 'Llama-3.2-1B (reference)', 'FlashAttention paper (Dao et al.)', 'torch.profiler', 'rotary_embedding reference impl']}
  device="colab"
  time="One focused weekend (~10 h)"
  level="advanced"
  steps={[
    {
      title: 'Set up the harness — Llama-3.2-1B on Colab',
      goal: 'Load Llama-3.2-1B-Instruct via transformers. Pull out one attention layer\'s weights (Q, K, V, O projections + RoPE freqs). Save them to a .pt file so you can reload without HF round-trips.',
      checkpoint: 'You can `torch.load("attn_layer_0.pt")` and get a dict with the right shapes for `(d_model=2048, H=32, H_kv=8, d_head=64)`.',
      trap: 'Llama-3.2-1B uses GQA — `num_key_value_heads=8` not `32`. Using full-MHA shapes will silently break shape checks two steps later.',
      est: '40 min',
    },
    {
      title: 'Naive multi-head attention — match the reference',
      goal: 'Write `naive_attention(x, weights)` in pure PyTorch: Q/K/V projections, head split, scaled dot-product, mask, softmax, output projection. No FlashAttention. Test on a 256-token input.',
      checkpoint: 'Your output matches `model.layers[0].self_attn(x)` to atol=1e-3. RMSE printed and small.',
      trap: 'Forgetting that GQA needs to *repeat* the K, V heads `n_query_heads / n_kv_heads = 4` times before the matmul. The reference does this internally; your reimplementation must too.',
      est: '90 min',
    },
    {
      title: 'Add RoPE — and compare against Llama\'s rotary_embedding',
      goal: 'Implement rotary positional embedding from scratch: build cos/sin tables for the head_dim, apply to Q and K before the attention matmul. Match the Llama-3.1 partial-rope and YaRN-style scaling if your model has it.',
      checkpoint: 'Output still matches reference to atol=1e-3 with positions > 0.',
      trap: 'Llama-3 uses partial RoPE — only the first `rotary_dim` of head_dim is rotated. Default config has `rotary_dim == head_dim`, but the partial case is the silent test that catches a wrong implementation.',
      est: '75 min',
    },
    {
      title: 'Tile it — write a FlashAttention-style forward',
      goal: 'Replace the inner attention with a block-tiled version: outer loop over Q blocks, inner loop over KV blocks, online softmax with running max + running sum. Pure PyTorch (no Triton yet) — block-tiled is enough to demonstrate the IO win.',
      checkpoint: 'Output still matches naive within atol=1e-3. The tiled implementation never materializes the full (N×N) score matrix.',
      trap: 'The classic bug: re-using the running-max from the previous block without rescaling the previous accumulator. The math is in the FlashAttention paper, equation (6); copy it carefully.',
      est: '120 min',
    },
    {
      title: 'Profile + benchmark — show the HBM traffic drop',
      goal: 'Use `torch.profiler` with `record_shapes=True` to capture HBM reads/writes for the naive vs tiled forward at seq_len 1024, 4096, 16384. Plot total bytes transferred per call.',
      checkpoint: 'Tiled version moves ~10× fewer bytes than naive at seq_len 16384. Plot saved to PNG.',
      trap: 'PyTorch profiler only catches HBM through CUDA events; running on CPU gives meaningless numbers. Do this on a Colab GPU.',
      est: '60 min',
    },
    {
      title: 'Push to GitHub with a clean README',
      goal: 'One-file repo: `attention_from_scratch.py` (your impl) + `bench.py` (the comparison) + `README.md` (one-paragraph description, the benchmark plot, the equation reference).',
      checkpoint: 'A reader can clone and run `pip install torch transformers matplotlib && python bench.py` and reproduce your plot.',
      est: '45 min',
    },
  ]}
  outcomes={[
    'A working attention block — MHA + GQA + RoPE + tiled forward — that matches a real model',
    'Fluency reading FlashAttention-style code: tiles, online softmax, the running-max trick',
    'A benchmark methodology you can re-use for any kernel comparison work',
    'A repo whose README + plot stand on their own',
  ]}
/>
